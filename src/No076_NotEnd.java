/*
 * From now on I will make the class nomenclature
 * as filename_number example: NotEnd_No076 because
 * it is easier to access what I am looking for on the command line.
 *
 * except these two nomenclature belong to the files
 * I downloaded from https://introcs.cs.princeton.edu/java/.
 * */
public class No076_NotEnd {
    public static void main(String[] args){
        System.out.println("From now on I will make the class " +
                "nomenclature as filename_number example: " +
                "NotEnd_No076 because it is easier to access " +
                "what I am looking for on the command line.");
    }
}
